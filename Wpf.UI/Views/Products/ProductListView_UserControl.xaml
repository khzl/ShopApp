<UserControl x:Class="Wpf.UI.Views.Products.ProductListView_UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wpf.UI.Views.Products"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900" Background="{StaticResource BackgroundBrush}">
    <Grid Margin="30">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,25">
            <StackPanel>
                <TextBlock Text="Product Management" 
                           Style="{StaticResource HeaderTextStyle}" FontSize="26"/>
                <TextBlock Text="View, Edit, and Search The List Of Available Products"
                           Style="{StaticResource BodyTextStyle}"/>
            </StackPanel>
            <Button Content="Add a New Product +"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Background="{StaticResource SuccessBrush}"
                    Width="160" Height="40"/>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}"
                 Padding="15" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔍"
                               VerticalAlignment="Center"
                               Margin="10,0"
                               Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBox Width="350" Height="35"
                             Tag="Search for The Product Name ...."
                             Text="{Binding SearchTerm , UpdateSourceTrigger=PropertyChanged}"
                             VerticalContentAlignment="Center"/>

                    <Button Content="Search"
                            Command="{Binding SearchCommand}"
                            Width="100" Margin="10,0"
                            Background="{StaticResource PrimaryBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}"
                Padding="0" 
                ClipToBounds="True">
            
            <DataGrid ItemsSource="{Binding Products}"
                  AutoGenerateColumns="False">
                <DataGrid.Columns>
                    
                    <!-- Header Columns -->
                    <DataGridTextColumn Header="ProductName" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding Price , StringFormat={}{0:C}}" Width="150"/>

                    <DataGridTemplateColumn Header="Status" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#E8F5E9"
                                        CornerRadius="10"
                                        Padding="8,2"
                                        HorizontalAlignment="Center">
                                    <TextBlock Text="Available"
                                               Foreground="{StaticResource SuccessBrush}"
                                               FontSize="11" FontWeight="Bold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Procedures" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="✏️"
                                            Background="Transparent"
                                            Foreground="{StaticResource PrimaryBrush}"
                                            Padding="5"
                                            Cursor="Hand"
                                            ToolTip="Edit"/>
                                    <Button Content="🗑️"
                                            Background="Transparent"
                                            Foreground="{StaticResource ErrorBrush}"
                                            Padding="5"
                                            Cursor="Hand"
                                            ToolTip="Delete"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
